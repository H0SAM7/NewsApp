class ArticleModel{
  String image;
  String title;
  String? subtitle;
  String url;
  ArticleModel({required this.image,required this.title,required this.subtitle,required this.url});//required this.url});

}